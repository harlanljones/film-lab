import {describe,expect,it} from 'vitest';import {addBeat,duplicatePlay,mirrorPlay,updateWaypoint,undoSnapshot} from '../editor';import {seededPlay} from '../../data/seededPlay';import {isValidPlay} from '../validate';
describe('editor operations',()=>{it('mirrors twice and remains valid',()=>expect(mirrorPlay(mirrorPlay(seededPlay))).toEqual(seededPlay));it('adds beats and keeps time order',()=>{const next=addBeat(seededPlay,{t:.2,title:'Read'});expect(next.beats).toHaveLength(3);expect(next.beats[1].t).toBe(.2)});it('edits waypoint timing and sorts',()=>{const next=updateWaypoint(seededPlay,'wr1',1,0);expect(next.tracks[0].waypoints[0].t).toBe(0);expect(isValidPlay(next)).toBe(true)});it('duplicates with new identity and undoes a snapshot',()=>{const copy=duplicatePlay(seededPlay,'copy');expect(copy.id).toBe('copy');expect(undoSnapshot(copy,seededPlay)).toEqual(seededPlay)});});
