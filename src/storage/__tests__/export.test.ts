import {describe,expect,it} from 'vitest';import {SCHEMA_VERSION,exportPlaybook,exportPlaybookDocument,importPlaybook,importPlaybookDocument,loadPlaybook,savePlaybook} from '../playbookStore';import {starterLibrary} from '../../data/library';
describe('playbook JSON interchange',()=>{it('round trips a playbook',()=>{const plays=starterLibrary.slice(0,2);expect(importPlaybook(exportPlaybook(plays))).toEqual(plays)});it('rejects invalid input without merging',()=>{expect(()=>importPlaybook(`{"schemaVersion":${SCHEMA_VERSION-1},"plays":[]}`)).toThrow(`schemaVersion ${SCHEMA_VERSION}`);});it('round trips concept-carrying plays and roster through a document file',()=>{const doc={schemaVersion:SCHEMA_VERSION,plays:starterLibrary.slice(0,3),sequence:[{playId:starterLibrary[0].id,reps:5}],roster:[{id:'r1',name:'Maya',number:7}]};const raw=exportPlaybookDocument(doc);savePlaybook(importPlaybookDocument(raw,{schemaVersion:SCHEMA_VERSION,plays:[],sequence:[],roster:[]}));expect(loadPlaybook()).toEqual(doc)});});
