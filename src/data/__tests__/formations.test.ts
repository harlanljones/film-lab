import {describe,expect,it} from 'vitest';import {defenseLooks,formationTemplates,offenseFormations} from '../formations';import {validateFormation} from '../../engine/validate';
describe('formation library',()=>{it('contains the required alignments and looks',()=>{expect(offenseFormations).toHaveLength(8);expect(defenseLooks).toHaveLength(7);expect(formationTemplates).toHaveLength(15)});it('sweeps every template',()=>{for(const template of formationTemplates)expect(validateFormation(template),template.name).toEqual([])});it('validates the expansion templates individually',()=>{const ids=['twins','bunch-right','trips-left','cover-1','cover-6'];const expanded=[...offenseFormations,...defenseLooks].filter((template)=>ids.includes(template.id));expect(expanded.map((template)=>template.id)).toEqual(ids);for(const template of expanded)expect(validateFormation(template),template.name).toEqual([])});});
